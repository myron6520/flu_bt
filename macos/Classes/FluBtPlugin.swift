import Cocoa
import FlutterMacOS

public class FluBtPlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {
    // This plugin uses dart:ffi directly. No method channel registration needed.
  }
}
